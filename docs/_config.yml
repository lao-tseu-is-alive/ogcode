title: ogcode
description: The token-efficient agentic coding workbench — built for a future where every token counts.
url: "https://ogcode.xyz"

theme: null

# Serve everything from docs/ as static files
# install.sh will be served as text/plain automatically by GitHub Pages
include:
  - install.sh
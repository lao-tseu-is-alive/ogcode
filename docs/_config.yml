title: ogcode
description: The context-engineered agentic coding workbench — it curates only the relevant context per turn, cutting 70%+ of tokens and sharpening accuracy so even lower-end models outperform frontier ones.
url: "https://ogcode.xyz"

theme: null

# Serve everything from docs/ as static files
# install.sh will be served as text/plain automatically by GitHub Pages
include:
  - install.sh